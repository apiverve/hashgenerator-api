name: apiverve_hashgenerator
description: Hash Generator is a tool for generating cryptographic hashes from text. It supports multiple algorithms including MD5, SHA-1, SHA-256, SHA-384, SHA-512, and BLAKE2 variants for data integrity and security applications.
version: 1.1.14
homepage: https://apiverve.com/marketplace/hashgenerator?utm_source=dart&utm_medium=homepage
repository: https://github.com/apiverve/hashgenerator-api/tree/main/dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  http_parser: ^4.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
